import React from 'react';

const TodoForm = () => {
    return (
      <div>
        <input type="text" placeholder="Add Task" />
        <button>Add</button>
      </div>
    );
}

export default TodoForm;
